import { connect } from 'react-redux';
import { {{pascalCase componentName}} as {{pascalCase componentName}}Component } from './{{pascalCase componentName}}';
import { AppState } from '../../reducers';

function mapStateToProps(state: AppState) {
  return {};
}

export const {{pascalCase componentName}} = connect(mapStateToProps)({{pascalCase componentName}}Component);
