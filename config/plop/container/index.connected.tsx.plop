import { connect } from 'react-redux';
import {{pascalCase containerName}} from './{{pascalCase containerName}}';
import { AppState } from '../../reducers';

function mapStateToProps(state: AppState) {
  return {};
}

export default connect(mapStateToProps)({{pascalCase containerName}});
