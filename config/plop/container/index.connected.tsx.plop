import { connect } from 'react-redux';
import { {{pascalCase containerName}} as {{pascalCase containerName}}Container } from './{{pascalCase containerName}}';
import { AppState } from '../../reducers';

function mapStateToProps(state: AppState) {
  return {};
}

export const {{pascalCase containerName}} = connect(mapStateToProps)({{pascalCase containerName}}Container);
