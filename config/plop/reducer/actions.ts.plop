import { action } from 'typesafe-actions'
import { {{pascalCase reducerName}}ActionTypes } from './types'

export const {{pascalCase reducerName}}Example = () => action(
  {{pascalCase reducerName}}ActionTypes.EXAMPLE
);
