import { DeepReadonly } from 'utility-types';

export type {{pascalCase reducerName}}State = DeepReadonly<{
  isLoading: boolean,
}>;

export enum {{pascalCase reducerName}}ActionTypes {
  EXAMPLE = '@@{{camelCase reducerName}}/EXAMPLE',
};
